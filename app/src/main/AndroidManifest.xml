<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Updated resource linking fixes -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VerticalParking">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.VerticalParking.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".ParkActivity" android:exported="false" />
        <activity android:name=".RetrieveActivity" android:exported="false" />

    </application>

</manifest>
